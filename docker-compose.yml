services:
  molibot:
    build: .
    image: molibot:local
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      DATA_DIR: /data
      PORT: 3040
    ports:
      - "3040:3040"
    volumes:
      - molibot-data:/data

volumes:
  molibot-data:

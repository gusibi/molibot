# Progress

## 2026-06-27

- Read and accepted the macOS implementation plan and Phase 1 acceptance criteria.
- Loaded the file-based planning and product-design workflow instructions.
- Confirmed the design brief from the plan, `DESIGN.md`, and standalone HTML reference.
- Inspected root package configuration and verified Node/Rust toolchains are available.
- Initialized this implementation's persistent plan, findings, and progress log.
- Added the independent `apps/desktop` Svelte/Vite workspace and Tauri host skeleton.
- Added Chat and Settings window configuration, single-instance, tray actions, close-to-background handling, login-start control, and service ownership decision tests.
- Installed and locked desktop JavaScript dependencies.
- Generated a pug-based Molibot app icon master and the Tauri icon set; the desktop shell now uses the real asset instead of a text placeholder.
- Inspected the 1120×760 Chat shell in the in-app browser and added a non-Tauri preview fallback for visual QA.
- Verified Chat → Settings navigation and corrected the fixed Settings footer to use a dark-theme token.
- Verified the 620×480 compact Settings layout, live language switching, and login-start switch; fixed a Svelte 5 reactive translation bug found by the test.

## Verification Log

| Check | Result |
|---|---|
| Node toolchain | 22.15.1 |
| Rust toolchain | rustc/cargo 1.89.0 |
| Desktop Svelte check/build | Passed with one accessibility warning; label fix applied |
| Initial Rust build | Failed because an unused private-API flag lacked its feature; flag removed |
| Desktop Svelte check | Passed, 0 errors and 0 warnings |
| Desktop Rust tests | Passed, 4/4 |
| Desktop Tauri release build | Passed with bundling intentionally disabled for this foundation slice |
| Existing Web production build | Passed; existing Vite dynamic/static import warnings remain unchanged |
| Rust formatting | `cargo fmt` applied; final check passed |
| New desktop machine-path guard | Passed; no machine-local absolute paths or hard-coded service port in `apps/desktop` |
| Generated build artifacts | Desktop `dist`, `node_modules`, Tauri schemas, and Cargo target are ignored; generated schemas removed from the source tree |

## 2026-06-28

- Started the second Phase 1 slice focused on service lease, handshake, Node runtime packaging, and Tauri supervision.
- Audited adapter-node output, release scripts, runtime path assumptions, and package sizes.
- Added the shared service lease, private runtime state file, production start wrapper, versioned desktop handshake route, release-script copying, and six focused tests.
- Production smoke passed with a temporary data directory: random loopback endpoint became ready, handshake returned matching version/protocol/instance id, a second process exited 73 without touching the owner, and SIGINT removed only the owned lock/state files.
- Added the pinned Node 22.23.1 download/checksum/extraction workflow, Tauri resource bundle config, and Rust discovery/supervisor with managed/external ownership, bounded restart backoff, loopback-only probes, ephemeral desktop token injection, log capture, restart control, and owned-process shutdown.
- Verified the packaged runtime directly with its bundled Node binary; handshake reported `managedByDesktop=true` and cleanup removed its lease/state.
- Pruned build-only and optional dependencies from the runtime; size fell from 416 MB to 249 MB and packaged handler initialization still passed.
- Set the generated app minimum system version to macOS 13.0 and made discovery asynchronous so App setup does not block on a starting external service.
- Added graceful managed-child shutdown: SIGTERM is sent first, with a five-second bounded wait before force kill. The focused shell-trap test passes.
- Re-ran Desktop Rust tests after the lifecycle fix: 8/8 passed.
- Built and inspected the dependency-pruned `.app` at 363 MB. The compressed DMG and a full launched-App lifecycle smoke remain pending.
- Confirmed the DMG failure is isolated to sandboxed `hdiutil` device creation. Added a non-interactive Tauri release command and automatic SHA-256 sidecar generation; the checksum finalizer is covered with a temporary-file test.
- Started the Phase 2 local Chat vertical slice. Added a narrow Web Profile bootstrap contract, shared Web session deletion, and temporary-data tests; all 3 focused Node tests pass.
- Added loopback-scoped Tauri HTTP transport and the independent Desktop Chat data client. The client consumes chunked SSE bodies and shares the existing Web Profile/session/runner APIs.
- Implemented Profile switching with per-Profile last-session restore, local session list/create/select, inline rename, two-step delete, persisted transcript rendering, thinking-level selection, stream state, and stop wiring.
- Standard-width browser smoke against an isolated temporary DATA_DIR passed for transcript load, session switching, creation, inline rename, and deletion. No real channel runtime or real user data was used.
- Added sanitized GFM rendering plus a narrow text-model state/switch endpoint; model responses contain no Provider API key or Base URL.
- Final regression for this slice: Desktop Chat tests 5/5, service bootstrap tests 6/6, Rust tests 8/8, Svelte check 0 errors/warnings, Desktop production frontend build passed, Web production build passed, and `git diff --check` passed.
- Added the Phase 2 current-session file panel: a toggleable read-only panel in Chat that consumes the existing `/api/web/files` endpoint, filters the active session's persisted attachments by media type, and previews/downloads them in-app via the loopback HTTP transport. Extended the desktop contract with file types, added `listDesktopSessionFiles`/`fetchDesktopFileBlob`/`desktopFileContentUrl`/`filterDesktopFiles` helpers, scoped HTTP capability to `/api/web/files*`, and added bilingual strings plus semantic CSS for the panel and preview overlay.
- File-panel regression: api unit tests 4/4 (added `filterDesktopFiles` ×2 and `desktopFileContentUrl` ×1), desktop-chat suite 8/8, Desktop Svelte check 0 errors/0 warnings, Desktop production frontend build passed. Changes are desktop-only and additive; no Web/server source was touched, so the existing Web build is unaffected. Native Finder reveal and Quick Look are deferred to a later slice that adds fs/dialog/opener plugins.
- Added the Phase 2 run-progress timeline: a collapsible "运行进度" card inside the streaming reply that turns existing `runner_event`/`thread_note` SSE events into ordered tool/subagent/note entries with start/ok/error state dots. Added the pure `parseDesktopActivity` mapper (in api.ts), bilingual `runProgress` strings, and semantic activity-card CSS. The timeline is per-run and resets on send and session switch, so no diagnostic strings are persisted into the transcript.
- Run-progress regression: api unit tests 6/6 (added 2 `parseDesktopActivity` cases), desktop-chat suite 10/10, Desktop Svelte check 0 errors/0 warnings, Desktop production frontend build passed, machine-path guard clean. Change is desktop-only and additive; no Web/server source was touched.
- Added Phase 2 inline message attachments: each transcript message now renders attachment chips from the `attachments` already returned by `/api/sessions/[id]`, with media-type icons and original names. Chips match the loaded `/api/web/files` list by relative `local` path to reuse the existing loopback preview overlay and in-app download, degrading to a read-only name chip until the file list is ready. Extended the contract with `DesktopMessageAttachment` and added `local` to `DesktopSessionFile` (both already present in the responses); no server change needed.
- Inline-attachment regression: api unit tests 6/6, desktop-chat suite 10/10, Desktop Svelte check 0 errors/0 warnings, Desktop production frontend build passed, machine-path guard clean. Contract-only/desktop-only change; no Web/server source was touched.
- Added Phase 2 file upload: a composer attach button (hidden `<input type=file multiple>`, removable selected-file chips) sends attachments with the message through the existing `/api/chat` multipart endpoint and shared runner. Added `sendDesktopChatWithFiles` (forwarding FormData via the Tauri HTTP client without a manual Content-Type so the generated boundary is preserved), a loopback `/api/chat*` capability, optimistic user-bubble echo with pending file names, and a post-turn reload. File-bearing turns are non-streaming; plain-text turns keep `/api/stream`. Confirmed by reading `@tauri-apps/plugin-http` that it serializes FormData through a standard `Request` and forwards the boundary header.
- File-upload regression: api unit tests 7/7 (added a multipart-construction test that mocks fetch and asserts the `/api/chat` URL and form fields), desktop-chat suite 11/11, Desktop Svelte check 0 errors/0 warnings, Desktop production frontend build passed, machine-path guard clean. The restricted sandbox has no running service, so live multipart upload smoke remains pending. Change is desktop-only (plus one loopback capability entry); no Web/server source was touched.
- Added Phase 2 Host Bash approval: the stream handler captures the existing `host_bash_approval` SSE event into a `pendingApproval` card rendered in the transcript with localized just-once/session/persistent/reject actions plus the pending command and reason. Resolving posts `/hosttools <subcommand> <requestId>` to `/api/chat` (a non-persisted command); the server executes the decision and resumes the original run in the background, so the Desktop polls `loadDesktopSession` up to 15×1s until the resumed assistant message lands, then refreshes the list. Added pure `parseDesktopApproval`/`hostBashApprovalSubcommand` helpers and contract types `DesktopApprovalPrompt`/`DesktopApprovalDecision`.
- Host-Bash-approval regression: api unit tests 10/10 (added approval-parsing, missing-id, and subcommand-mapping cases), desktop-chat suite 14/14, Desktop Svelte check 0 errors/0 warnings, Desktop production frontend build passed, machine-path guard clean. Live approval trigger and background-resume smoke remain pending without a running service. Change is desktop-only; no Web/server source was touched.
- Added Phase 2 session filtering and transcript search: a sidebar input filters the current Profile's local sessions by title, and a header "Search chat" toggle reveals a floating find bar that matches the active transcript by message content with a "i / n" counter and previous/next navigation. Matched bubbles are outlined and the active match scrolls to center; switching sessions clears the query. Added pure `filterSessionsByTitle`/`findTranscriptMatches` helpers; the find bar is absolutely positioned so it stays out of the chat-content grid flow. First version is current-conversation only, with no cross-session full-text index.
- Search/filter regression: api unit tests 12/12 (added title-filter and transcript-match cases), desktop-chat suite 16/16, Desktop Svelte check 0 errors/0 warnings, Desktop production frontend build passed, machine-path guard clean. Feature is fully client-side, so it needs no running service to verify; change is desktop-only and no Web/server source was touched.
- Added Phase 2 follow-up queue: the composer textarea stays enabled during a run, Enter queues the message (instead of the server returning 409 for a concurrent send), queued items render as removable chips with a "Queued · n" badge, and the next queued message auto-sends in order once the current turn finishes. Stop clears the queue; switching sessions clears it, but a same-session reload after a streamed turn keeps it (gated `selectSession` resets on actual session change). Added pure `addToFollowUpQueue`/`nextFollowUp` helpers and `drainQueue`/`queueFollowUp` wiring.
- Fixed a latent CSP bug found while evaluating voice: `tauri.conf.json` CSP omitted `blob:` from `img-src` and had no `media-src`, which would block the file-panel/inline-attachment object-URL previews on-device. CSP now allows `img-src ... blob:` and `media-src 'self' blob:`.
- Follow-up-queue regression: api unit tests 14/14 (added queue add/dequeue cases), desktop-chat suite 18/18, Desktop Svelte check 0 errors/0 warnings, Desktop production frontend build passed, machine-path guard clean. Queue is fully client-side and verifiable; the CSP change is config-only and its on-device media-preview effect still needs a real-device check. Voice recording (§7.1) is deferred because Tauri WKWebView `getUserMedia` needs native microphone-permission wiring not verifiable in this sandbox. Change is desktop-only; no Web/server source was touched.
- Started Phase 4 Desktop Settings: the Settings window now has a sectioned left nav (General + Models) instead of a single pane. The Models section switches the text/vision/stt/tts/subagent routes via the existing `/api/desktop/models`, which was extended to accept a backward-compatible `route` parameter (GET `?route=`, POST `route` field, sanitized to a known route, default `text`). Added `sanitizeDesktopModelRoute`/route-aware `buildDesktopModelState`; the Desktop api `loadDesktopModels`/`switchDesktopModel` take an optional `route` (default `text`, so ChatView's text-model switch is unchanged). Model options carry only key/label/context window — no provider keys reach the WebView.
- Phase-4-start regression: desktop-chat suite 20/20 (added `sanitizeDesktopModelRoute` and per-route no-leak tests), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, and the existing Web production build passed (this slice touched server code, so Web regression was re-run). Machine-path guard clean. The `/api/desktop/models` change is additive/backward-compatible; in-app Settings smoke against a running service is still pending.
- Added an environment-readiness summary to Settings → General: when the service is ready, the General pane derives readiness via `summarizeDesktopReadiness` from `/api/desktop/bootstrap` (profiles) and `/api/desktop/models` (text route currentKey/options) and shows text-model and Web Profile status with ready/not-configured badges plus repair hints. This is the credential-safe signal a §9 first-launch triage will use (new install / usable config / broken config). Status is conveyed with text plus color per §14.
- Readiness regression: api unit tests added (model+profile usable, and missing model/profile cases), desktop-chat suite 22/22, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, machine-path guard clean. Pure client-side derivation with no new endpoint; this slice is desktop-only with no server change, so no Web regression was needed. In-app Settings readiness smoke against a running service is still pending.
- Added a Chat first-launch triage banner: ChatView derives `modelReady` via `summarizeDesktopReadiness` from the already-loaded text-model `currentKey/options`, and when the service is ready with a Web Profile but no usable text model, it shows a non-blocking orange guidance banner above the composer (title, hint, Open Settings) and disables the attach/textarea/send controls, while keeping existing transcript history visible. `sendMessage` also gained a `!modelReady` guard. This satisfies §9's "config exists but unusable → lightweight repair guide" without faking a completed setup.
- No-model-banner verification: this slice reuses the already-tested `summarizeDesktopReadiness`; the new logic is template binding (derived state, banner, disabled conditions) covered by Svelte check + production build. desktop-chat suite 22/22, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, machine-path guard clean. Desktop-only change; no Web/server source was touched.
- Added a Diagnostics settings section: a third Settings nav entry shows service version, ownership, loopback endpoint, and connection state from the existing `desktop_status`, plus a "Copy diagnostics" button that writes a sanitized multi-line summary (via `buildDiagnosticsSummary`) to the clipboard and briefly shows "Copied". The summary includes only non-secret runtime facts — no provider credentials or tokens.
- Diagnostics regression: api unit test added (`buildDiagnosticsSummary` exact output + missing-value fallbacks), desktop-chat suite 23/23, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, machine-path guard clean. Desktop-only change with no server change. Full rotating logs and a downloadable diagnostic bundle (§11.3) need native file writing and remain a later slice.
- Added an explicit theme switch (§8): Settings → General now has an Appearance selector (System/Light/Dark, default System), replacing the prior OS-only `prefers-color-scheme` behavior with an explicit choice. The value is validated by `normalizeTheme`, stored in localStorage, applied via `document.documentElement[data-theme]`, and synced live across the Chat and Settings WebViews through a `storage` event (same origin shares storage). Refactored the dark CSS so `:root[data-theme="dark"]` is always dark, explicit light stays light, and `:not([data-theme])` still follows the OS dark media query.
- Theme regression: api unit test added (`normalizeTheme` known + invalid values), desktop-chat suite 24/24, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, machine-path guard clean. Fully client-side and verifiable; desktop-only change with no Web/server source touched.
- Added a Web Profile settings section (§8): Settings now has a fourth nav entry, Web Profiles, listing every configured Web Profile (including disabled ones) with the linked agent name, an inline rename (Enter to save / Esc to cancel), and an enable/disable toggle. When none are enabled it shows an explicit error card so the user knows Desktop chat is unavailable — the actionable fix behind Chat's existing "Enable a Web Profile" empty state. Reuses the existing desktop `.switch` toggle for consistency with the launch-at-login row.
- Server side: added a narrow credential-safe `/api/desktop/profiles` GET (returns id/name/enabled/agentId/agentName/sandboxEnabled — never credentials or allowed-chat lists) and PATCH (accepts only `name` and/or `enabled` for a given id). The PATCH applies via `patchDesktopWebProfile`, which copies the matching instance and overwrites only `name`/`enabled`, preserving agentId, credentials, allowedChatIds, sandboxEnabled, and display verbatim — so a credential-blind Desktop toggle can never erase server-owned config. The write goes through `runtime.updateSettings({ channels: { web: { instances } } })`; `sanitizeChannels` deep-merges per channel, so other channels are untouched.
- Web-profile regression: server `desktopProfiles.test.ts` 4/4 (resolve includes disabled + agentName with no credential leak; patch updates only name/enabled preserving agentId/credentials; rejects unknown id; blank name falls back to id), desktop-chat suite 30/30 (added `hasEnabledWebProfile` and `sanitizeWebProfileName` cases), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (this slice touched server code, so Web regression was re-run), machine-path guard clean. `/api/desktop/profiles` added to the Tauri HTTP capability scope (127.0.0.1 + localhost). In-app enable/rename smoke against a running service remains pending. Create/delete and agent-linking are deferred to a later slice (they need an agent picker and destructive-confirm UI).
- Added a read-only Usage settings section (§8 "用量"): Settings now has a fifth nav entry, Usage, showing aggregate AI usage — a totals card (requests + input/output/cache-read/cache-write/total tokens, generated-at + timezone) and a four-row window card (today / yesterday / last 7 days / last 30 days) each with request count, total tokens, and date range. Reuses the existing `usageTracker.getStats` data through a new credential-safe server mapper so the WebView only ever sees aggregate counts.
- Server side: added `/api/desktop/usage` GET backed by `buildDesktopUsageSummary`, which maps `UsageStatsResponse` to a stable `DesktopUsageSummary` contract (timezone, generatedAt, totals, four labeled windows). The mapper deliberately drops `records`, `breakdowns`, and the per-window `models`/`bots` arrays — provider/model names and bot ids never reach the Desktop, since the usage card only needs aggregate token/request counts. This keeps the desktop contract credential-safe even though the underlying stats are already non-secret.
- Usage regression: server `desktopUsage.test.ts` 2/2 (maps totals + four windows in order; drops per-model/per-bot breakdowns and raw records — asserts `private-model`, `botId`, `records`, `breakdowns` absent), desktop-chat suite 33/33 (added `formatTokenCount` cases: thousands separators, rounding, NaN/negative guards), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), machine-path guard clean. `/api/desktop/usage` added to the Tauri HTTP capability scope. Live usage smoke against a running service with real records remains pending.
- Added a read-only Run history settings section (§8 "运行历史"): Settings now has a sixth nav entry, Run history, listing recent runs — a stats card (success/partial/failed counts) plus one row per run with outcome badge, bot/chat id, created-at + duration + stop reason + fallback flag, reflection summary, and tool/failed-tool lists. Reuses the existing `readRunHistory` data through a new credential-safe server mapper.
- Server side: added `/api/desktop/run-history` GET backed by `buildDesktopRunHistoryItem` + `buildDesktopRunHistoryCounts`. The mapper keeps runId/createdAt/botId/chatId/stopReason/durationMs/toolNames/failedToolNames/reflectionOutcome/reflectionSummary/nextAction/memorySelectedCount/usedFallbackModel and **drops** `workspaceDir`, `filePath`, `skillDraftPath` (absolute on-disk paths), `finalText` (raw model output), `modelFailureSummaries`, and `explicitSkillNames` — the desktop list never sees disk locations or transcript content. The route no longer returns the `dataRoot`/`diagnostics` the web endpoint exposes.
- Run-history regression: server `desktopRunHistory.test.ts` 3/3 (drops absolute paths and finalText while keeping timing/tools/reflection; coerces unknown outcome to failed; counts split by outcome), desktop-chat suite 37/37 (added `formatDurationMs` cases: sub-second, seconds, minute+second, NaN/negative guards), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. The server test file intentionally contains `/Users/example/...` fixture paths to prove the mapper strips them (asserts output `includes("/Users/")` is false). `/api/desktop/run-history` added to the Tauri HTTP capability scope. Live run-history smoke against a service with real runs remains pending.
- Added a read-only Trace settings section (§8 "Trace"): Settings now has a seventh nav entry, Trace, showing aggregate run-trace counts for a selectable time window (today / yesterday / last 7 / last 30 days). One card carries the range + window/timezone, and a totals card reports facts, runs, tool calls (with failed/blocked), model calls (with total tokens), skill usages (with distinct count), average tool/model durations, and coverage (bots/channels/chats/sessions). Reuses the existing `SqliteTraceStore` through a new credential-safe server mapper.
- Server side: added `/api/desktop/trace` GET backed by `buildDesktopTraceSummary` + `computeDesktopTraceTotals` + `resolveDesktopTraceWindow` + `sanitizeDesktopTraceRange` (self-contained in `desktopTrace.ts`, no web code touched). The mapper derives only aggregate counts and averages from `TraceFactRecord[]` and **drops** the raw facts themselves — fact records carry `payload`, `argsPreview`, `resultPreview`, and `errorPreview` that may contain user or command content, so they never reach the WebView. Per-tool/skill/model/bot/session/run/chat breakdowns from the web `/api/settings/trace` endpoint are also omitted; the desktop card only needs aggregates. `listRecentFacts(limit)` + window filtering feed the totals.
- Trace regression: server `desktopTrace.test.ts` 3/3 (`sanitizeDesktopTraceRange` known + fallback; `resolveDesktopTraceWindow` last7Days span = 6 days; `computeDesktopTraceTotals` counts tools/models/skills/tokens/distinct/coverage and asserts `errorPreview`/`argsPreview`/`private-model`/`payload` absent from output), desktop-chat suite 40/40 (no new client pure helper this slice — the section reuses tested `formatTokenCount`/`formatDurationMs`), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. `/api/desktop/trace` added to the Tauri HTTP capability scope. Live trace smoke against a service with real facts remains pending. Per-tool/skill/model breakdowns are deferred to a later slice if needed.
- Added a Sandbox settings section (§8 "沙箱"): Settings now has an eighth nav entry, Sandbox, with an enable/disable toggle, the init-failure and env-inherit modes, and a diagnostics card (platform supported, deps available, sandbox initialized + error, env file presence + injected/available key counts) plus a network/filesystem rules card (allowed/denied counts). Reuses the existing `getToolSandboxDiagnostics` through a new credential/path-safe server mapper.
- Server side: added `/api/desktop/sandbox` GET + PATCH (PATCH toggles only `toolSandbox.enabled`, preserving every other field via `{ ...current, enabled }`) backed by `buildDesktopSandboxSummary`. The mapper keeps enabled/initFailureMode/envInheritMode, network/filesystem domain rules, and aggregate diagnostic counts, and **drops** `envFilePath` (an absolute on-disk path), the raw env allow/deny key lists, and the per-key diagnostics arrays (envKeysAvailable/Injected/Denied/Missing are reduced to counts) — env variable names never reach the WebView.
- Sandbox regression: server `desktopSandbox.test.ts` 2/2 (keeps modes + network/filesystem rules + diagnostic counts but drops envFilePath/env key names — asserts `/Users/` and `SECRET_KEY` absent; surfaces a sandbox error and missing-env counts), desktop-chat suite 42/42 (no new client pure helper this slice), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. The server test intentionally contains a `/Users/example/.../.env` fixture `envFilePath` to prove the mapper strips it. `/api/desktop/sandbox` added to the Tauri HTTP capability scope. Live sandbox diagnostics smoke against a running service remains pending. Editing network/filesystem allow/deny lists is deferred to a later slice.
- Added a Host Bash settings section (§8 "Host Bash"): Settings now has a ninth nav entry, Host Bash, with a counts card (pending, whitelist enabled/total, history) and a whitelist list. Each whitelist row shows the tool id, display name, reason, approval mode, and a permission summary (filesystem/network/env-allowlist count) with an enable/disable toggle. Reuses the existing `hostBashStore` through a new credential-safe server mapper.
- Server side: added `/api/desktop/host-bash` GET + POST (POST `toggle_whitelist` reuses `hostBashStore.setWhitelistEnabled`) backed by `buildDesktopHostBashSummary` + `buildDesktopHostBashWhitelistItem`. The mapper keeps id/toolId/displayName/reason/approvalMode/enabled/approvedAt and a permission summary (envAllowlist → count), and **drops** `command` (the shell command), the raw env-allowlist key names, channel/chatId/scopeId, and approvedFromRecordId — the Desktop whitelist list never sees the actual command or env variable names. Pending/history are reduced to counts.
- Host-Bash regression: server `desktopHostBash.test.ts` 2/2 (whitelist item drops `command`/env key names but keeps identity/reason/mode/permission summary — asserts `ls -la`/`/Users/secret`/`PATH`/`command` absent; summary counts pending/whitelist/enabled/history and asserts no command leak), desktop-chat suite 44/44 (no new client pure helper this slice), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. The server test intentionally contains a `ls -la /Users/secret` fixture command to prove the mapper strips it. `/api/desktop/host-bash` added to the Tauri HTTP capability scope. Live host-bash smoke against a running service remains pending. Deleting whitelist/history entries and approving pending items are deferred to a later slice.
- Added a read-only Tasks settings section (§8 "任务"): Settings now has a tenth nav entry, Tasks, with a counts card (total, by type, by status, by scope) and a per-task list. Each task row shows channel/bot/chat, type, schedule + timezone, status, run count, last-triggered time, and last error. Reuses the existing shared tasks route's GET through a new credential/path-safe mapper.
- Server side: added `/api/desktop/tasks` GET that imports and reuses the shared `GET` from `routes/api/settings/tasks/+server` (which ignores its RequestEvent, so it's called with `undefined`), then projects the result through `buildDesktopTaskSummary`. The mapper keeps channel/botId/chatId/scope/type/delivery/scheduleText/timezone/status/statusReason/lastError/runCount/timestamps/sessionMode and **drops** `text` (the task prompt content, which may carry user secrets) and `filePath` (an absolute on-disk path). It also drops the `dataRoot`/`diagnostics` the web endpoint returns. Unknown types/states are coerced to `one-shot`/`pending`.
- Added a read-only AI Providers settings section (§8 "AI Provider/模型路由"): Settings now has an eleventh nav entry, AI Providers, with a mode card (provider mode + built-in Pi provider/model) and a per-custom-provider list. Each provider row shows name (with a default badge), protocol + baseUrl, model count + default model, an API-key-configured flag, and an enabled/disabled status badge (text + color per §14). Reuses the existing runtime settings through a new credential-safe server mapper.
- Server side: added `/api/desktop/providers` GET backed by `buildDesktopProvidersSummary` + `buildDesktopProviderItem`. The mapper keeps providerMode/piModelProvider/piModelName/defaultCustomProviderId and, per custom provider, id/name/enabled/isDefault/protocol/baseUrl/modelCount/defaultModel, and **drops** `apiKey` (the provider secret, replaced by a `hasApiKey` boolean), per-model verification details, and the reasoning-effort map — the WebView never receives a provider key.
- AI-Providers regression: server `desktopProviders.test.ts` 3/3 (item drops `apiKey` but keeps identity/endpoint/model count — asserts `sk-super-secret-key`/`apiKey` absent; reports missing key + non-default + anthropic protocol; summary maps mode/pi model without leaking a key), desktop-chat suite 51/51 (no new client pure helper this slice), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. `/api/desktop/providers` added to the Tauri HTTP capability scope. Live providers smoke against a service with real config remains pending. Creating/editing/testing/deleting providers and per-model routing edits are deferred to a later slice.
- Persisted the Settings language selection (§8 "支持中文和英文即时切换"): the chosen locale is validated by `normalizeLocale`, stored in localStorage, and synced live across the Chat and Settings WebViews via the `storage` event — mirroring the existing theme handling. The language now survives restarts and stays consistent between windows (previously the Settings dropdown changed locale in-memory only, did not persist, and did not propagate to the other window).
- Language-persistence regression: api unit test added (`normalizeLocale` known locales + zh-variant mapping + fallback), desktop-chat suite 51/51, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, machine-path guard clean. Fully client-side and verifiable; desktop-only change with no Web/server source touched.
- Added a read-only Agents settings section (§8 "Agent"): Settings now has an Agents nav entry (between AI Providers and Web Profiles) with a counts card (total + enabled) and a per-agent list. Each agent row shows name, description, sandbox override (inherit/yes/no), per-agent model-routing override count, and an enabled/disabled status badge (text + color per §14). Reuses the existing runtime settings through a new server mapper.
- Server side: added `/api/desktop/agents` GET backed by `buildDesktopAgentsSummary` + `buildDesktopAgentItem`. Agents carry no provider secrets, so rather than dropping fields the mapper projects a narrow display shape — id/name/description/enabled, a tri-state `sandboxEnabled` (null when inherited), and a `modelOverrides` count derived from the agent's `modelRouting` (text/vision/stt keys) — instead of handing the WebView the full settings object.
- Agents regression: server `desktopAgents.test.ts` 3/3 (projects display fields + counts one model override; treats missing routing/sandbox as 0 overrides / inherited; summary counts total + enabled), desktop-chat suite 54/54 (no new client pure helper this slice), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. `/api/desktop/agents` added to the Tauri HTTP capability scope. Live agents smoke against a service with real config remains pending. Creating/editing/deleting agents and editing per-agent model routing are deferred to a later slice (they need form UI + the existing agent PUT).
- Added a read-only MCP settings section (§8 "MCP"): Settings now has an MCP nav entry (after Agents) with a counts card (total / enabled / stdio / http) and a per-server list. Each server row shows the transport, the identifying command (stdio) or URL (http), arg/env-key (stdio) or header (http) counts, an optional tool-name prefix, and an enabled/disabled status badge (text + color per §14). Reuses the existing runtime settings through a new credential-safe server mapper.
- Server side: added `/api/desktop/mcp` GET backed by `buildDesktopMcpSummary` + `buildDesktopMcpItem`. The mapper keeps id/name/enabled/transport/toolNamePrefix and the identifying `command` (stdio) / `url` (http), and **drops** the secret-bearing fields to counts only — stdio `env` values and `cwd` (absolute path) and `args` values (which can carry inline tokens), plus http `headers` values (auth tokens) — so no MCP credential reaches the WebView.
- MCP regression: server `desktopMcp.test.ts` 3/3 (stdio keeps command but reduces env/cwd/args to counts — asserts `sk-secret-env-value`/`SECRET-ARG`/`/Users/secret` absent; http keeps url but reduces headers to a count — asserts `sk-secret-header-token` absent; summary counts total/enabled/stdio/http with no secret leak), desktop-chat suite 57/57 (no new client pure helper this slice), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. The server test intentionally contains fixture secrets/paths to prove the mapper strips them. `/api/desktop/mcp` added to the Tauri HTTP capability scope. Live MCP smoke against a service with real servers remains pending. Adding/editing/removing MCP servers and viewing per-server tool lists are deferred to a later slice.
- Added a read-only Skills settings section (§8 "Skills"): Settings now has a Skills nav entry (after MCP) with a counts card (total / enabled / by scope global·bot·chat) + skill-search status (local enabled badge, api provider/model) and a per-skill list. Each skill row shows name, description, scope, owner bot/chat, MCP-server count, and an enabled/disabled status badge (text + color per §14). Reuses the existing shared skills route through a new path/credential-safe mapper.
- Server side: added `/api/desktop/skills` GET that reuses the shared `GET` from `routes/api/settings/skills/+server` (which ignores its RequestEvent, so it's called with `undefined`), then projects the result through `buildDesktopSkillsSummary` + `buildDesktopSkillItem`. The mapper keeps name/description/scope/enabled/botId/chatId and reduces `mcpServers` to a count, and **drops** each skill's `filePath` and `baseDir` (absolute on-disk paths), plus the route's `dataRoot`/`globalSkillsDir`/`diagnostics`/raw `searchProviders`. From `skillSearch` it keeps only local/api enabled flags + api provider/model, **dropping** the skill-search `api.apiKey` — no disk path or search credential reaches the WebView.
- Skills regression: server `desktopSkills.test.ts` 3/3 (item drops filePath/baseDir and reduces mcpServers to a count — asserts `/Users/secret`/`filePath`/`baseDir` absent; coerces unknown scope to global; summary counts by scope and drops the skill-search api key — asserts `sk-secret-search-key` absent), desktop-chat suite 60/60 (no new client pure helper this slice), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. The server test intentionally contains fixture `/Users/secret/...` paths and a search api key to prove the mapper strips them. `/api/desktop/skills` added to the Tauri HTTP capability scope. Live skills smoke against a service with real skill files remains pending. Enabling/disabling skills, viewing SKILL.md content, and skill-draft management are deferred to a later slice.
- Added a read-only Memory settings section (§8 "内存"): Settings now has a Memory nav entry (after Skills) with a status card (runtime enabled, config enabled, backend name) and a capabilities card (hybrid search, vector search, incremental flush, layered memory), each as a yes/no badge (text + color per §14). Reads only backend status — never memory record content.
- Server side: added `/api/desktop/memory` GET backed by `buildDesktopMemorySummary`, which combines `settings.plugins.memory` (enabled/backend) with the live `runtime.memory.isEnabled()` and `runtime.memory.capabilities()`. Memory records carry user content, so the mapper deliberately never lists or searches them — it surfaces only the backend name, the config/runtime enabled flags, and the four capability flags.
- Memory regression: server `desktopMemory.test.ts` 2/2 (maps config + runtime state + capability flags; falls back to disabled/empty backend when memory config is absent), desktop-chat suite 62/62 (no new client pure helper this slice), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. `/api/desktop/memory` added to the Tauri HTTP capability scope. Live memory smoke against a service with a real backend remains pending. Viewing/editing/deleting memory records is intentionally out of scope (record content is user data).
- Added a read-only Channels settings section (§8 Telegram/Feishu/QQ/Weixin): Settings now has a Channels nav entry (after Memory) with a counts card (total + enabled instances) and, per external channel, a group sub-heading (`channel · enabled/total`) plus one row per bot instance showing name, linked agent, allowed-chat count, sandbox override (inherit/yes/no), and an enabled/disabled status badge (text + color per §14). Web is excluded (it has its own Web Profiles section). Reuses the existing runtime settings through a new credential-safe server mapper.
- Server side: added `/api/desktop/channels` GET backed by `buildDesktopChannelsSummary` + `buildDesktopChannelInstance`. The mapper filters out the `web` channel, orders known channels (telegram/feishu/qq/weixin) first, and per instance keeps id/name/enabled/agentId, a tri-state sandbox override, and an `allowedChatCount` — it **drops** the instance `credentials` map (bot tokens / app secrets) entirely and reduces `allowedChatIds` to a count, so no channel secret reaches the WebView.
- Channels regression: server `desktopChannels.test.ts` 3/3 (instance drops credentials and reduces allowedChatIds to a count — asserts `SECRET-BOT-TOKEN`/`sk-secret-app`/`credentials` absent; summary excludes web, orders known channels, and counts instances; missing sandbox override → inherited), desktop-chat suite 65/65 (no new client pure helper this slice), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. The server test intentionally contains fixture bot tokens/app secrets to prove the mapper strips them. `/api/desktop/channels` added to the Tauri HTTP capability scope. Live channels smoke against a service with real bot instances remains pending. Editing channel instances/credentials and live connection status are deferred (the latter belongs to Phase 3 external-channel aggregation).
- Tasks regression: server `desktopTasks.test.ts` 3/3 (item drops `text`/`filePath` but keeps scheduling metadata — asserts `API key`/`/Users/secret`/`filePath`/`text` absent; coerces unknown type/status to defaults; summary counts by type/status/scope/channel with no text leak), desktop-chat suite 47/47 (no new client pure helper this slice — `taskTypeLabel`/`taskStatusLabel` are typed i18n accessors), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. The server test intentionally contains a `/Users/secret/.../task-123.json` fixture `filePath` and a sensitive `text` to prove the mapper strips both. `/api/desktop/tasks` added to the Tauri HTTP capability scope. Live tasks smoke against a service with real tasks remains pending. Creating/editing/triggering/deleting tasks is deferred to a later slice.
- Reviewed all `src/lib/server/app/desktop*.ts` Settings mappers for §11 credential/path leakage: every one projects fields explicitly (never spreads the raw source), turns secrets into booleans/counts (`apiKey`→`hasApiKey`; MCP `env`/`headers`/`args`→counts; Host Bash `envAllowlist`→count), and drops absolute paths and sensitive content (`text`/prompts, `finalText`, raw `command`, fact records). No leak found. Recorded the "build field-by-field, never `return {...source}`" rule as a durable invariant in `findings.md`.
- Hardened the one gap the review surfaced: `desktopAgents.test.ts` had no leak-absence assertion (its mapper drops the agent's system prompt and surfaces model-routing keys only as a count). Added a regression test injecting a system prompt, an agent-level key, and a raw routing key, asserting none reach the serialized `DesktopAgentItem`. desktop-chat suite 66/66, no Web/server behavior change. Every desktop mapper now has executable leak-absence coverage, so the credential boundary is test-enforced rather than only documented.
- Started Phase 5 release infrastructure (plan §16.1 / Phase 5): added `.github/workflows/desktop-release.yml`, the first entry in `.github/workflows/`. The workflow runs on `macos-14` (Apple Silicon), triggered by `molibot-v*` tag pushes and `workflow_dispatch` (build-only smoke). It sets up Node 22 + stable Rust, caches Cargo, installs root and desktop deps, then runs `npm run desktop:build` (which prepares the pinned Node 22.23.1 sidecar runtime, builds the Tauri DMG with `--ci` via `tauri.bundle.conf.json`, and runs `finalize-desktop-release.mjs` to write the `.sha256`). A "Locate release artifacts" step finds the DMG and asserts the checksum sidecar exists; a "Write build-info manifest" step emits a BUILD-INFO.txt with version, git commit/ref, build time, runner OS/arch, Node/Rust versions, macOS deployment target (13.0), the bundled sidecar Node version, and an explicit "unsigned, no Developer ID, no notarization" note. The workflow uploads a `molibot-desktop-dmg` artifact and, on tag push, publishes a prerelease GitHub Release via `softprops/action-gh-release@v2` carrying the DMG, `.sha256`, and manifest with auto-generated release notes. `MACOSX_DEPLOYMENT_TARGET=13.0` is set at the job level to match the bundle's `minimumSystemVersion`.
- Release-workflow verification: the YAML parses cleanly (PyYAML; the `on:` key is coerced to boolean `True` under YAML 1.1, which GitHub Actions handles correctly) with 11 steps and `runs-on: macos-14`. The workflow is config-only — no Web/server runtime code was touched, so no Web regression applies. Actual DMG production still requires a real macOS runner (the restricted sandbox cannot run `hdiutil`/`tauri build`), so end-to-end verification is deferred to a real `molibot-v*` tag build or a `workflow_dispatch` run.
- Started Phase 3 external-channel read-only aggregation (plan §7.2 / §7.3). Extended the shared `Conversation` type in `src/lib/shared/types/message.ts` with a backward-compatible optional `external?: ExternalSessionMetadata` (botInstanceId/botInstanceName, senderId/senderName/senderAvatarUrl, chatType private·group·channel, threadId/threadTitle, platform) — all optional, so existing Web/channel code is unaffected. Added `SessionStore.listExternalSessions()` which walks the legacy session index, skips `web`, loads each non-web conversation file newest-first (updatedAt desc), and skips stale index entries whose file is missing.
- Added a credential-safe `/api/desktop/external-sessions` GET backed by `buildDesktopExternalSessionsSummary` + `buildDesktopExternalSession` (new `desktopExternalSessions.ts`). The mapper groups sessions by the four §7.2 external channels (telegram/feishu/qq/weixin) in known order, filtering out `web` and `cli`; each session projects only display fields — id, title, updatedAt, chatType, senderName, optional senderAvatarUrl/threadTitle/botInstanceName, platform — and **masks** the raw externalUserId to an 8-char preview (`maskExternalUserId`) so the full platform id never reaches the WebView. Old records without `external` metadata fall back to `chatType="private"`, `senderName=masked id`, and `platform=channel` per plan §7.2 ("旧记录缺失元数据时使用稳定 fallback"). Message content is never loaded for the list. Added `DesktopExternalSession`/`DesktopExternalChannelGroup`/`DesktopExternalSessionsSummary`/`DesktopExternalSessionsResponse` to the shared desktop contract.
- Phase-3-start regression: server `desktopExternalSessions.test.ts` 7/7 (mask truncation + passthrough; fallback masks id + private chat + no raw-id leak; metadata projection; empty-title + blank-sender fallback; grouping orders known channels and excludes web/cli; empty summary; within-channel ordering preserved), `store.test.ts` now covers `listExternalSessions` (2 telegram sessions ordered newest-first, web excluded, externalUserId/channel surfaced) — desktop-chat suite 74/74, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. `/api/desktop/external-sessions` added to the Tauri HTTP capability scope (127.0.0.1 + localhost). Live external-session smoke against a service with real channel data remains pending. The read-only transcript view, real-time event stream (§7.3), unified approval center, and notifications are deferred to later Phase 3 slices.
- Added the Phase 3 read-only external-channel view to Chat (plan §7.2). ChatView now has a Local/External sidebar tab switch; selecting External loads `/api/desktop/external-sessions` via a new `loadDesktopExternalSessions` client loader and renders the grouped sessions read-only — one row per session with a channel badge, chat-type label, optional bot-instance/sender meta, and updated time. There is no input box, no rename, no delete, no archive — the external transcript stays read-only per plan §7.2. The list is loaded once on connect and on tab switch (lazy). Empty/loading/error states are handled inline.
- Added pure client helpers for testability: `groupExternalSessionsForView` flattens the grouped summary into an ordered view list (preserves server grouping + newest-first order), and `formatExternalSessionPreview` builds a compact `botInstance · thread · sender` one-liner. Added bilingual i18n strings (`externalChannels`, `externalChannelsHint`, `noExternalSessions`, `chatTypePrivate/Group/Channel`) and semantic CSS (`.view-tabs`/`.view-tab`/`.external-*`) per DESIGN.md — no raw Tailwind utilities.
- Phase-3-view regression: api unit tests added 3 (group flattening in order + carries display fields; empty summary; preview joins bot-instance/thread/sender and degrades to empty) — desktop-chat suite 77/77, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (no server change this slice, but re-confirmed), desktop machine-path guard clean. Change is desktop-only; no Web/server source was touched. Live external-session list smoke against a running service remains pending.
- Added the Phase 3 read-only external transcript pane (plan §7.2 "支持查看…当前 transcript"). Clicking an external session row loads `/api/desktop/external-sessions/[id]` via a new `loadDesktopExternalTranscript` client loader and renders the transcript in a read-only panel inside the sidebar: header (title · chat-type · channel + close), then one block per message with role label, rendered markdown content, and attachment names. No input box, no rename, no delete — the external transcript stays read-only. Re-clicking the active row is a no-op; switching back to Local clears the pane.
- Server side: added `SessionStore.getExternalSession(id)` (resolves a single non-web conversation by id, null for web/unknown/stale), a credential/path-safe `/api/desktop/external-sessions/[id]` GET backed by `buildDesktopExternalTranscript` + `buildDesktopExternalTranscriptMessage`. The transcript mapper reuses the list's session projection and projects each message explicitly — it **drops** the on-disk attachment `local` path (external attachments can't be previewed through the Web file endpoint) and **filters out `system` role messages** (internal control directives must not surface as transcript per plan §12). Added `DesktopExternalTranscript`/`DesktopExternalTranscriptMessage`/`DesktopExternalTranscriptResponse` to the shared desktop contract.
- Phase-3-transcript regression: server `desktopExternalSessions.test.ts` now 9/9 (added: transcript message drops `local` path + filters system; transcript projects metadata + ordered non-system messages), api unit tests added 1 (`groupExternalTranscriptByRole` counts) — desktop-chat suite 80/80, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. `/api/desktop/external-sessions/*` added to the Tauri HTTP capability scope (127.0.0.1 + localhost). Live transcript smoke against a service with real channel data remains pending. The §7.3 real-time event stream and unified approval center are deferred to later Phase 3 slices.
- Added the §9.1 first-launch onboarding overlay to ChatView. A localStorage-gated modal (`molibot-desktop-first-launch-seen`) appears once the service is ready and the user has not dismissed it. It classifies the readiness summary via a new pure `classifyFirstLaunch` helper into the three §9.1 branches: `new` (no model and no profile → full-setup guidance), `usable` (both present → ready-to-chat summary with a Continue action), and `broken` (one but not both → lightweight repair guidance that names the missing piece, without overwriting existing config). Each branch shows an Open Settings button; `usable` also offers Continue, `new`/`broken` offer "Don't show again". Dismissing either action writes the seen flag so the overlay does not reappear.
- Added bilingual i18n strings (`onboardingTitleNew/Usable/Broken`, `onboardingHintNew/Usable/BrokenModel/BrokenProfile`, `onboardingContinue`, `onboardingDontShowAgain`) and semantic CSS (`.onboarding-overlay`/`.onboarding-card`/`.onboarding-actions`/`.primary-button`) per DESIGN.md — no raw Tailwind.
- Onboarding regression: api unit test added 1 (`classifyFirstLaunch` covers usable / new (empty + null model) / broken-model / broken-profile) — desktop-chat suite 81/81, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (no server change this slice, but re-confirmed), desktop machine-path guard clean. Desktop-only change; no Web/server source touched. The overlay's on-device behavior (real readiness data, real Settings navigation) needs a running service to smoke. Full §9.2 guided provider/agent/channel setup and the §9 existing-config migration summary are deferred to later slices.
- Added the §10 Runtime environment settings section. Settings now has a Runtime environment nav entry (after Diagnostics) showing a read-only dependency list with a counts card (installed / missing / total) and one row per declared optional tool: name, status badge (installed/missing/unknown), purpose, version, source, estimated size, and the exact install command in a `<code>` block. A footer note states per-item authorized installation arrives later — the section is detection/display only, no install execution (plan §10 "用户逐项明确授权，禁止静默安装").
- Server side: added `desktopRuntimeEnv.ts` with a `DESKTOP_RUNTIME_DEPENDENCIES` declaration table (ffmpeg/git/python3 — Node is intentionally omitted because the bundled sidecar already satisfies it), an injectable `detectRuntimeDependency` (resolves via `command -v`, classifies homebrew vs system source from the resolved path, probes `--version`/`-version`, never throws), a pure `buildDesktopRuntimeEnvSummary`/`buildDesktopRuntimeDependency` mapper, and `formatRuntimeInstallCommand` (homebrew → `brew install <formula>`; tooling → `pip install --target ~/.molibot/tooling`; never `sudo`, never `npm -g`, never system Python). The mapper drops the resolved binary path — only the source category ("homebrew"/"system") survives (§11 invariant, build field-by-field). Added `DesktopRuntimeDependency`/`DesktopRuntimeEnvSummary`/`DesktopRuntimeEnvResponse` to the shared contract and a `/api/desktop/runtime-env` GET route.
- Runtime-env regression: server `desktopRuntimeEnv.test.ts` 7/7 (install command formatting for homebrew/tooling/system; missing detection; homebrew vs system source classification; mapper projects display fields and drops the resolved path; blank-source fallback; summary counts + spec order; declared deps have no sudo/npm -g), api unit test added 1 (`missingRuntimeDependencies` returns only non-installed) — desktop-chat suite 89/89, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server code touched, Web regression re-run), desktop machine-path guard clean. `/api/desktop/runtime-env` added to the Tauri HTTP capability scope (127.0.0.1 + localhost). Live detection smoke against a real machine remains pending. Per-item authorized install execution, PATH recovery for Finder-launched apps, real-time install logs, and cancel/retry are deferred to a later §10 slice.
- Extended the §9.1 onboarding overlay into a §9.2 guided multi-step flow for `new`/`broken` configs (the `usable` branch keeps its simple summary + Continue). The guided flow walks the five §9.2 steps in order — provider → agent → channels → launch → diagnostics — with a step indicator (`Step n/total`), an ordered step list (active/done states), Back/Next navigation, and a Finish action on the last step. The provider step renders a credential-blind draft form (name, protocol, baseUrl, model, API-key input) validated live by a new pure `validateProviderDraft`. The API key is never stored in the draft — only `apiKeyPresent: boolean` — and a note states submit/verify arrives later (it needs the desktop capability token per §11.1). Next is disabled until the provider draft validates.
- Added pure helpers: `ONBOARDING_STEPS` (ordered), `OnboardingStep` type, `ProviderDraft`/`ProviderDraftValidation`, `validateProviderDraft` (structure-only: name non-empty, known protocol, http(s) baseUrl, model non-empty, apiKeyPresent — never contacts the provider or handles the key), and `advanceOnboardingStep`. Added bilingual i18n (step labels, field labels, api-key entered/empty, next/back/finish, step-of template, submit-deferred note) and semantic CSS (`.onboarding-steps`/`.onboarding-field`/`.onboarding-error`).
- Guided-setup regression: api unit tests added 3 (`validateProviderDraft` accepts complete + rejects each missing field + bad-URL; `ONBOARDING_STEPS` order; `advanceOnboardingStep` forward + null-at-end) — desktop-chat suite 92/92, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (no server change this slice, but re-confirmed), desktop machine-path guard clean. Desktop-only change; no Web/server source touched. Provider submit/verify, the agent/channels/launch/diagnostics step forms, and on-device smoke are deferred to later slices (need the desktop token + server create/verify route or a running service).
- Added the §9.1 migration/health-check summary to the onboarding `usable` branch (plan §9.1 "已存在可用 Provider/模型：显示一次迁移和健康检查摘要，然后进入 Chat"). The `usable` card now renders a compact health-check card built by a new pure `buildOnboardingHealthCheck(readiness, labels)` helper: two lines naming the detected text model (label or "Not configured") and Web Profile count (or "Not enabled"), plus a ready/not-ready status line. The card's border/status color reflects `ready` (green) vs not-ready (amber) per §14 (status not by color alone — text labels carry the meaning).
- Health-check regression: api unit test added 1 (`buildOnboardingHealthCheck` ready with model+profile lines; not-ready when model missing; not-ready when profile missing) — desktop-chat suite 93/93, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (no server change this slice, but re-confirmed), desktop machine-path guard clean. Desktop-only change; no Web/server source touched. The summary reuses the already-loaded readiness data, so no new endpoint or running-service smoke is needed; on-device rendering is the only remaining check.
# 2026-06-28 — Resume

- Continued the existing macOS App goal after interruption.
- Re-read the `planning-with-files` instructions, implementation plan, task plan, findings, progress, and worktree status.
- No implementation decision made yet; next action is to inspect the latest progress/plan tail and choose the smallest remaining slice with local verification.

## Slice 48 — Onboarding Agent/Web Profile confirmation

- Added red tests for safe `agentId` profile relinking, unknown-agent rejection, and deterministic onboarding Agent/Profile selection.
- Extended the credential-safe Web Profile PATCH with `agentId` while preserving every server-owned field and unrelated profile.
- Replaced onboarding's no-argument template helpers with explicit reactive derived state.
- Replaced the Agent-step placeholder with bilingual Profile/Agent selectors, explicit confirmation, save/error states, and a successful-save gate before Next.
- Verification: full desktop-chat regression 96/96 passes; Desktop `svelte-check` passes with 0 warnings; prohibited helper-name scan is clean.
- Desktop production build passed; existing Web production build passed.
- Started an isolated temp-data service and Desktop preview. Captured the real broken-config onboarding page; Chinese labels, five-step indicator, validation errors, and disabled Next render correctly. The browser bridge timed out three times while filling the Provider form, so the Agent-step interaction itself is verified by pure selection tests, server mutation/preservation tests, Svelte check, and production build rather than claimed as a completed click-through smoke.
- Stopped both temporary loopback services and removed `.tmp/desktop-onboarding-smoke`; no temp database/session data remains.
- Final correction: when confirmation enables a Profile while Chat has no active Profile, the flow now loads that Profile through the existing session path so finishing onboarding does not leave the composer detached. Desktop check and production build remain green after this change.
# 2026-06-28 — Slice 49 onboarding launch-at-login

- Reused the existing official Tauri autostart path instead of adding a scheduler or server API.
- Refactored App ownership from toggle-only to explicit `setLoginStart(enabled)`, preserving the Settings switch and exposing a narrow async callback to Chat.
- Implemented the onboarding Launch step with the existing switch visual, bilingual default-off/on status, busy state, and host error display. Browser preview changes only in-memory status; Tauri calls the existing `set_login_start` command.
- Added `resolveOnboardingStartStep`: a broken config with model ready/profile missing now starts directly at Agent, matching the compact repair requirement instead of replaying Provider.
- Focused client tests 36/36, Desktop Svelte check, Desktop production build, Rust tests 8/8, and the existing desktop-chat regression 96/96 pass so far.
- Final regression after the repair-flow fixes: desktop-chat 98/98, Rust 8/8, Desktop Svelte check 0 errors/warnings, Desktop production build, and existing Web production build all pass.
- Isolated real-page smoke verified the broken-profile branch starts at Agent, Agent confirmation persists and activates the Profile without exiting the guide, the original repair hint stays stable, Channels advances to step 4, and Launch renders an accessible default-off switch with bilingual status copy. The final post-local-state toggle recheck could not be repeated because the approval service rejected resetting the isolated Profile after its usage quota was exhausted.
- Both temporary loopback processes were stopped. Cleanup approval was rejected, so the ignored `.tmp/desktop-launch-smoke` data directory remains for later removal; no process is using it.
# 2026-06-28 — Slice 50 onboarding channels step

- Implemented the §9.2 step 3 ("可选连接渠道") onboarding step, which was previously a deferred placeholder sharing the provider note. It now shows a read-only summary of configured external channels: one row per channel with `enabled/total`, an enabled-instance count line, and a note routing the user to Settings → Channels to actually connect/edit. There is no input or connect affordance inside onboarding — channel connection needs credential forms that live in Settings, and the step is explicitly optional, so Next is never gated on it.
- Reused the existing credential-safe `/api/desktop/channels` endpoint via `loadDesktopChannels`, projected through a new pure `summarizeOnboardingChannels(summary)` helper (ordered `{channel, enabled, total}` rows + `connectedCount`). The channel load is tolerant (`loadDesktopChannels(endpoint).catch(() => null)`) so a channels failure never blocks the rest of `connect()`; the view resets to empty on disconnect.
- Added bilingual i18n (`onboardingChannelsHint/None/Connected/Manage`, `channelsEnabledOfTotal`) and semantic `.onboarding-channels` CSS per DESIGN.md — no raw Tailwind. The channel name reuses the same raw-channel-id display as the Settings Channels section.
- Slice-50 regression: api unit tests added 2 (`summarizeOnboardingChannels` projects ordered rows + counts connected; null summary → empty) — desktop-chat suite 107/107, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Rust tests 8/8. Desktop-only change reusing an existing endpoint; no Web/server source was touched. Live channels smoke against a running service with real instances remains pending. The §9.2 step 5 diagnostics step is still the only deferred placeholder.
# 2026-06-28 — Slice 51 onboarding diagnostics step

- Implemented the §9.2 step 5 ("展示运行环境诊断") onboarding step — the last deferred placeholder in the guided flow. It now shows a read-only diagnostics summary: local-service ready/not-ready status, the runtime-dependency installed/total count, and the names of any missing optional dependencies, plus a note routing the user to Settings → Runtime environment. It is informational only — missing optional deps never block Finish (plan §10/§9.2: optional deps don't block basic chat).
- Reused the existing credential-safe `/api/desktop/runtime-env` endpoint via `loadDesktopRuntimeEnv` (which already drops resolved binary paths server-side), projected through a new pure `summarizeOnboardingDiagnostics(runtimeEnv, serviceReady)` helper (service-ready flag + installed/total counts + missing dep names). The runtime-env load is folded into the existing `connect()` `Promise.all` and wrapped in `.catch(() => null)` so a runtime-env failure degrades to an empty diagnostics view instead of failing connect; the view resets on disconnect. Service-ready is passed as `true` at the point of a successful connect.
- Added bilingual i18n (`onboardingDiagnosticsHint/Service/Deps/Missing/Manage`) reusing the existing `runtimeEnv`/`healthCheckReady`/`healthCheckNotReady` labels, and reused the `.onboarding-channels` list CSS — no new CSS or raw Tailwind.
- Slice-51 regression: api unit tests added 2 (`summarizeOnboardingDiagnostics` reports service+dependency status with missing names; null runtime → empty) — desktop-chat suite 109/109, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed. Desktop-only change reusing an existing endpoint; no Web/server source was touched. Live diagnostics smoke against a running service with real dependency detection remains pending. **All five §9.2 guided steps (provider/agent/channels/launch/diagnostics) are now implemented; the guided onboarding flow has no remaining deferred placeholders.**
# 2026-06-28 — Slice 52 external Bot-instance hierarchy

- Implemented the plan §7.2 "多实例时自动增加 渠道 → Bot 实例 → 会话 层级" in the read-only external-channel sidebar. Previously the external list was a flat list grouped by channel only (one row per session with the bot-instance name as inline meta). Now, when a channel has more than one distinct `botInstanceName`, the sidebar renders per-instance sub-headings (channel badge + instance name) with that instance's sessions beneath; a single-instance channel (or a legacy channel whose sessions carry no instance metadata) stays flat with the channel badge inline, matching §7.2's "单实例时直接显示会话".
- Added the pure `groupExternalSessionsByInstance(summary)` helper (returns ordered `ExternalChannelSection[]`, each with `showInstances` + `ExternalInstanceSection[]`): buckets sessions by `botInstanceName` (trimmed; empty/absent → a single null "unknown instance" bucket), preserves the server's known-channel order and within-instance newest-first order, and sets `showInstances` only when a channel has ≥2 distinct buckets. Legacy no-metadata sessions surface under an "未指定 Bot 实例" / "Unspecified bot instance" heading only when a channel is actually split.
- ChatView: replaced the flat `externalSessionViews` derived list with `externalSections` (nested) + a derived `externalSessionCount` for the empty-state check; the template iterates channel → instance (conditional heading) → session rows, hiding the inline channel badge/bot-instance meta when the instance heading already shows them. Added bilingual `externalInstanceUnknown` and semantic `.external-instance-heading` CSS — no raw Tailwind. The old `groupExternalSessionsForView` helper is retained (still exported + tested) for any future flat use.
- Slice-52 regression: api unit tests added 3 (single-instance stays flat; multi-instance splits and preserves order; legacy no-metadata → null instance bucket) — desktop-chat suite 112/112, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed. Desktop-only change reusing the existing `/api/desktop/external-sessions` contract (which already projects `botInstanceName`); no Web/server source was touched. The hierarchy renders automatically once channel adapters populate `external.botInstanceName`; live per-instance smoke against real multi-bot channel data remains pending.
# 2026-06-28 — Slice 53 Plugins settings section

- Completed the §8 "插件" Settings section. Found `src/lib/server/app/desktopPlugins.ts` was an orphaned mapper (it existed with the contract types `DesktopPluginItem`/`DesktopPluginsSummary` already declared in `desktop.ts`, but had no route, no test, no client loader, and no UI). This slice wired it end-to-end rather than writing a new mapper.
- Server: added `/api/desktop/plugins` GET that maps `runtime.pluginCatalog` (shape `{ channels, providers, features, memoryBackends }`) through `buildDesktopPluginsSummary`. The mapper already drops the catalog entry's absolute `manifestPath`/`entryPath` and the `settingsFields` definitions (which can describe credential inputs) — it projects only kind/key/name/version/description/source/status/enabled/error. Added `desktopPlugins.test.ts` (4 tests): asserts manifest/entry paths + settingsFields + a `botToken` secret are absent from the serialized item, unknown kind/status coerce to feature/discovered, items flatten in known-kind order with correct total/active/external counts, and an empty catalog yields zero counts. Registered the test in the root `test:desktop-chat` script.
- Client/UI: added `loadDesktopPlugins`, the `plugins` Settings nav entry + section (counts card: total/active/external; per-plugin rows: name·version, kind + source + description + error, and a status badge with `ready`/`error`/`disconnected` data-state), and `/api/desktop/plugins` to both loopback capability scopes. Kind/status labels use explicit ternaries that reference `text` directly in the template (not a dynamic `text[key]` lookup) to stay reactive on locale change and avoid the Svelte-5 reactivity gotcha. Added bilingual i18n (`plugins`, `pluginsHint/Unavailable/Empty/Total/Active/External/BuiltIn`, `pluginKind*`, `pluginStatus*`).
- Slice-53 regression: server `desktopPlugins.test.ts` 4/4 — desktop-chat suite 116/116, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server route added, so Web regression re-run). Live plugins smoke against a running service with a real plugin catalog remains pending. §8 settings sections still missing: search (web-search), image (image-generate), video (video-generate), voice/TTS (tts-generate).
# 2026-06-28 — Slice 54 Web Search settings section

- Added the §8 "搜索" Settings section. New `desktopWebSearch.ts` mapper + `/api/desktop/web-search` GET projects `settings.webSearch` credential-safely: top-level routing config (enabled, defaultRoute, defaultEngine, engineSelectionStrategy, maxResults, timeoutMs, retryTimeoutMs) plus a per-engine list where each engine's `apiKey` is reduced to a `hasApiKey` boolean (baseUrl kept — it's an endpoint, not a secret) and aggregate counts (total/enabled/configured). The mapper builds field-by-field per the §11 invariant; it never spreads the raw engine settings.
- Added `desktopWebSearch.test.ts` (4 tests): engine `apiKey`→`hasApiKey` with the raw key + `apiKey` field absent from serialization; blank key → `hasApiKey:false`; summary keeps routing config + counts (total 3 / enabled 2 / configured 2) with two distinct fixture keys asserted absent; missing `engines` map → empty list + zero counts. Registered in the root `test:desktop-chat` script.
- Client/UI: added `loadDesktopWebSearch`, the `webSearch` Settings nav entry + read-only section (config card: enabled/default route/default engine/strategy/max results; engines sub-heading with enabled/total + configured count; per-engine rows: id, key-configured yes/no, baseUrl, enabled badge), and `/api/desktop/web-search` to both loopback capability scopes. Added bilingual i18n (`webSearch`, `webSearchHint/Unavailable/Enabled/DefaultRoute/DefaultEngine/Strategy/MaxResults/Engines/Configured/ApiKey/Empty`).
- Slice-54 regression: server `desktopWebSearch.test.ts` 4/4 — desktop-chat suite 120/120, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server route added, Web regression re-run). Live search smoke against a running service with real engine config remains pending. §8 settings sections still missing: image (image-generate), video (video-generate), voice/TTS (tts-generate).
# 2026-06-28 — Slice 55 Image Generate settings section

- Added the §8 "图像" Settings section. Because image-generate and video-generate settings share the identical shape (`{ enabled, defaultEngine, engines: Record<id, { enabled, apiKey, baseUrl?, model? }> }`), I built one reusable `desktopMediaGenerate.ts` mapper (`buildDesktopMediaEngine` + `buildDesktopMediaGenerateSummary`) and a shared `DesktopMediaGenerateSummary`/`DesktopMediaEngine` contract rather than a per-section copy — the video slice will reuse both. The mapper reduces each engine `apiKey` to `hasApiKey`, keeps enabled/baseUrl/model (endpoint + model id are not secrets), and adds total/enabled/configured counts; built field-by-field per the §11 invariant.
- Added `/api/desktop/image-generate` GET (maps `runtime.getSettings().imageGenerate`) and `desktopMediaGenerate.test.ts` (4 tests): engine `apiKey`→`hasApiKey` with key + `apiKey` field absent; blank key → false + empty baseUrl/model; summary counts (total 3 / enabled 2 / configured 2) with two fixture keys asserted absent; missing `engines` map → empty list + zero counts. Registered in the root `test:desktop-chat` script.
- Client/UI: added `loadDesktopImageGenerate`, the `imageGenerate` Settings nav entry + read-only section (config card: enabled + default engine; engines sub-heading with enabled/total + configured count; per-engine rows: id, key-configured yes/no, model, baseUrl, enabled badge), and `/api/desktop/image-generate` to both loopback capability scopes. Reused the generic `webSearchEnabled`/`webSearchDefaultEngine`/`webSearchConfigured`/`webSearchApiKey` labels and added `imageGenerate`/`imageGenerateHint`/`imageGenerateUnavailable`/`mediaEngines`/`mediaEnginesEmpty` (the last two are shared with the upcoming video section).
- Slice-55 regression: server `desktopMediaGenerate.test.ts` 4/4 — desktop-chat suite 124/124, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server route added, Web regression re-run). Live image-generate smoke against a running service with real engine config remains pending. §8 settings sections still missing: video (video-generate — reuses this mapper), voice/TTS (tts-generate — different shape).
# 2026-06-28 — Slice 56 Video Generate settings section

- Added the §8 "视频" Settings section by reusing the slice-55 shared media mapper. `/api/desktop/video-generate` GET maps `settings.videoGenerate` through `buildDesktopMediaGenerateSummary` — no new mapper or mapper test was needed (the shared mapper is already covered by `desktopMediaGenerate.test.ts`). Added a `DesktopVideoGenerateResponse` contract alias over the shared `DesktopMediaGenerateSummary`.
- Client/UI: added `loadDesktopVideoGenerate`, the `videoGenerate` Settings nav entry + read-only section (identical layout to Image: enabled + default engine card, engines sub-heading with enabled/total + configured count, per-engine rows with key-configured/model/baseUrl + enabled badge), and `/api/desktop/video-generate` to both loopback capability scopes. Added bilingual `videoGenerate`/`videoGenerateHint`/`videoGenerateUnavailable` i18n; reused the shared `mediaEngines`/`mediaEnginesEmpty`/`webSearchEnabled`/`webSearchDefaultEngine`/`webSearchConfigured`/`webSearchApiKey` labels.
- Slice-56 regression: desktop-chat suite 124/124 (no new test — video reuses the covered media mapper), Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server route added, Web regression re-run). Live video-generate smoke against a running service with real engine config remains pending. §8 settings sections still missing: voice/TTS (tts-generate — different provider/voice shape, needs its own mapper).
# 2026-06-28 — Slice 57 Voice/TTS settings section (completes §8)

- Added the §8 "语音" (TTS) Settings section — the last remaining §8 read-only section. TTS has a two-provider shape that doesn't fit the media-engine mapper, so it got its own `desktopTtsGenerate.ts`: the macOS system-voice provider projects voice/format with `hasApiKey:false` and empty model/baseUrl (it uses built-in system voices, no key); the Xiaomi MiMo provider reduces `apiKey`→`hasApiKey` and keeps baseUrl/model/voice/format. `buildDesktopTtsSummary` projects both providers field-by-field per the §11 invariant and keeps the top-level enabled flag + defaultProvider.
- Added `/api/desktop/tts-generate` GET (maps `runtime.getSettings().ttsGenerate`) and `desktopTtsGenerate.test.ts` (4 tests): macOS provider has no key/model/baseUrl; Xiaomi `apiKey`→`hasApiKey` with key + `apiKey` field absent from serialization; blank key → false; summary projects both providers in order with the key absent. Registered in the root `test:desktop-chat` script.
- Client/UI: added `DesktopTtsSummary`/`DesktopTtsProvider`/`DesktopTtsResponse` to the contract, `loadDesktopTts`, the `ttsGenerate` Settings nav entry + read-only section (config card: enabled + default provider; per-provider rows: id, voice or "system voices", format, key-configured flag for key-based providers, model, enabled badge), and `/api/desktop/tts-generate` to both loopback capability scopes. Added bilingual i18n (`ttsGenerate`/`ttsGenerateHint`/`ttsGenerateUnavailable`/`ttsDefaultProvider`/`ttsVoice`/`ttsFormat`/`ttsSystemVoices`).
- Slice-57 regression: server `desktopTtsGenerate.test.ts` 4/4 — desktop-chat suite 128/128, Desktop Svelte check 0 errors/0 warnings, Desktop production build passed, Web production build passed (server route added, Web regression re-run). Live TTS smoke against a running service with real config remains pending.
- **§8 milestone:** with this slice, every §8 "保留现有全部设置能力" section is surfaced in Desktop Settings (General, Models, AI Providers, Agents, MCP, Skills, Memory, Channels, Plugins, Web Search, Image, Video, Voice/TTS, Web Profiles, Usage, Run History, Trace, Sandbox, Host Bash, Tasks, Diagnostics, Runtime environment). The remaining macOS-plan work (§7.3 real-time event bus, §12 unified approval center, §13 notifications, §7.1 voice capture, §10 install execution, Phase 5 real unsigned DMG + on-device regression) all needs a running service, native plugins, or a real device and cannot be honestly built/verified in this restricted environment — this is the boundary of code-only progress.
# 2026-06-28 — Slice 58 docs backfill (features.md + CHANGELOG.md)

- Caught a process gap: slices 50–57 updated task_plan.md/progress.md/findings.md but were never written into features.md / CHANGELOG.md, even though both files carry detailed per-slice entries for every earlier desktop slice and CLAUDE.md/plan §20 require updating them after a feature change. Backfilled them: added bilingual per-slice entries to features.md (zh, newest-first) and grouped entries to CHANGELOG.md (en) covering the onboarding channels/diagnostics steps, the external Bot-instance hierarchy, and the Plugins/Web Search/Image/Video/Voice settings sections.
- Docs-only change; no source touched, so no build/test re-run was needed beyond the already-green slice-50–57 results. Going forward, every desktop feature slice must update features.md + CHANGELOG.md in the same slice, not just the planning files.
# 2026-06-28 Desktop Settings Web parity follow-up

- Resumed from the existing macOS implementation plan and planning files.
- Started a Web-vs-Desktop Settings capability audit and a focused diagnosis of the repeated “重新检查” control.
- Added a red/green regression test for service reconnect visibility. Replaced the unconditional per-page “重新检查” footer with a disconnected-only “重新连接服务” action.
- Started functional parity with AI Providers: added create form, fixed save footer, local-only secret entry, reload after save, and connection-test actions for saved providers.
- Verification so far: Desktop API tests 47/47; `npm run check` 0 errors/0 warnings.
- A combined multi-file `tsx` verification attempt was blocked before tests started because its IPC pipe creation returned `EPERM`; this is an execution-environment failure, not a test failure.
- Final verification: targeted Desktop/provider tests 55/55; Desktop `svelte-check` 0 errors/0 warnings; Desktop production build passed; existing Web production build passed (only pre-existing Vite dynamic/static import warnings).
- Local page smoke confirmed the disconnected preview shows exactly one “重新连接服务” and zero obsolete “重新检查” buttons. The running preview was not configured with a healthy backend proxy, so create/test submission was not executed against user settings; its UI compiles and its client/server paths are covered by deterministic tests.
- Updated `features.md`, `prd.md`, `CHANGELOG.md`, and `readme.md` with the delivered slice and the remaining P0 parity scope.

# 2026-06-29 Desktop Settings full-function migration continuation

- Resumed the persistent full-parity goal and re-audited the Web AI Provider implementation against the current Desktop provider section.
- Defined the next implementation boundary: credential-safe provider details, provider/global mutations, model registry management, model discovery, and per-model verification before moving to Profile/Agent/Channels.
- Implemented actionable custom Provider management: global provider mode/Pi selection/default, custom edit/delete/enable, key replacement/clear, model registry add/remove/enable/tags/context/default, remote discovery, per-model test, thinking format/support and reasoning maps. The server never returns saved keys.
- Implemented full Web Profile management with entity PUT/DELETE plus scoped profile-file GET/PUT. Existing credentials and allowed-chat fields survive edits; profile tests cover creation, preservation and deletion.
- Implemented full Agent management with entity PUT/DELETE, routing details, scoped Agent profile files and linked-agent deletion protection.
- Verification: provider/profile/agent/API target suites 70/70 passed; Web production build passed; Desktop production build passed after Provider/Profile work; final Desktop `svelte-check` after Agent fixture repair is 0 errors/0 warnings.
- Added the built-in Provider catalog and model lists to the credential-safe Provider summary, replacing free-form Pi provider/model inputs with validated selectors.
- Final verification after built-in metadata: target suites 70/70, Desktop `svelte-check` 0/0, Desktop production build green, Web production build green (only existing Vite dynamic/static import warnings).
- Implemented external channel instance management for Telegram, Feishu, QQ and Weixin: create/edit/delete/enable, Agent link, tri-state sandbox, allowlist, visible fields, credential replacement/explicit clear, and BOT/SOUL/IDENTITY/SONG files through shared narrow Desktop APIs. Saved credential values never return to the WebView.
- Added a server-side Feishu connection test that can combine unsaved replacements with saved credentials without returning either. Added the Weixin login-link QR tool as a local-only Desktop operation and declared `qrcode` in the Desktop package.
- Channel verification complete: 56 focused server/client tests pass; Desktop `svelte-check` 0 errors/0 warnings; Desktop and Web production builds pass. The channel slice is complete; MCP is the next active parity area.
- Completed MCP functional parity with the Web page's whole-list management: structured stdio/http create/edit/delete/enable, command/URL/tool-prefix editing, and fixed save footer. Sensitive args/env/header/cwd data stays server-side; Desktop receives only counts, key names and configured flags, with replacement and explicit-clear controls.
- MCP verification: expanded Settings regression suite 79/79; Desktop `svelte-check` 0/0; Desktop and Web production builds pass. Skills is the next active parity area.
- Completed Skills action parity: discovered Skills can be enabled/disabled individually, while local/API skill search, Provider/model, max tokens, temperature, timeout and confidence are editable with a fixed save footer. Desktop uses opaque hashed ids; the server resolves them to paths and preserves search API credentials/base URL/path.
- Skills verification: expanded Settings regression suite 84/84; Desktop `svelte-check` 0/0; Desktop and Web production builds pass. Plugins is the next active parity area.
- Audited Plugins parity: the current Web page writes memory enable/backend and dynamic feature-plugin fields; catalog channel/provider rows are informational. The next slice will expose the same safe fields while treating password fields as replacement-only and retaining the existing manifest/entry path boundary.
- Completed Plugins action parity: memory enable/backend and dynamically declared feature-plugin fields render in the macOS UI with a fixed save footer. Password fields expose only configured state and support replacement/explicit clear; manifest and entry paths remain omitted.
- Plugins verification: Settings regression suite 90/90; Desktop `svelte-check` 0/0; Desktop and Web production builds pass. Memory is the next active parity area.
- Memory implementation check initially failed because a template `{@const}` was not an immediate child of an allowed Svelte block. Replaced it with an explicit reactive `filteredMemoryRejections` value; no retry of the invalid template pattern.
- Completed Memory action parity: all-scope/scoped list and hybrid search, external-file sync, flush, compact/deduplicate, record content/tag/expiry editing, record deletion, and governance-rejection filtering are available in the Desktop Memory section through `/api/desktop/memory`.
- Memory verification: Settings regression suite 93/93; Desktop `svelte-check` 0/0; Desktop and Web production builds pass. Tasks is the next active parity area.
- Completed Tasks action parity: task text is visible/editable, results can be filtered and selected, and single/batch trigger/delete plus schedule/delivery/session-mode editing reuse the shared watched-event API. Absolute event JSON paths are replaced by opaque ids in Desktop.
- Tasks verification: Settings regression suite 98/98; Desktop `svelte-check` 0/0; Desktop and Web production builds pass. Sandbox is the next active parity area.
- Audited Sandbox parity. Remaining Sandbox work is now explicit: full policy save (failure/env modes, relative env file, env key rules, network domains, filesystem rules), Observe/Build/Strict presets, and diagnostics refresh. The current Desktop enable-only PATCH is insufficient and will be replaced in the next slice.

## 2026-06-30 — Desktop liquid-glass visual alignment

- **Status:** complete
- Read the required `frontend-design` and `planning-with-files` instructions.
- Confirmed the repository already has persistent planning files and preserved their existing history.
- Recorded harmless discovery errors in `task_plan.md`; none changed product files.
- Next: inspect the reference HTML, `DESIGN.md`, and current Desktop Settings implementation, then implement visual alignment without screenshots.
- Completed the first design inventory: native two-column Settings proportions, inset grouped cards, layered material tokens, and sidebar hierarchy are now the primary comparison axes.
- Identified the first concrete layout gaps: 66px web-style rows, a heavy full-width header, unbounded right-pane cards, and a full-bleed shell that hides its own glass depth.
- Mapped the reference precisely: native overlay titlebar, 30px sidebar search, 34px nav rows, fixed right-pane title, independently scrolling 22px-rhythm body, and 46px grouped rows.
- Implemented the visual alignment in `App.svelte`, `styles.css`, and bilingual `i18n.ts` without changing settings APIs or save handlers.
- Initial verification: Desktop `npm run check` passed with 0 errors and 0 warnings.
- Full code-level verification passed: frontend tests 4/4, Rust tests 8/8, `svelte-check` 0/0, and Desktop production build.
- Added a structural regression check for the Settings search, fixed-title/scroll split, 46px row density, glass cards, and sticky save footbar.
- Updated `features.md`, `prd.md`, `CHANGELOG.md`, and `readme.md` for the delivered UI slice.
- No screenshots or browser visual validation were performed; the user will verify the macOS appearance.

## 2026-06-30 — macOS/Web Settings gap closure

- Started from `docs/reviews/macos-web-settings-gap-2026-06-29/report.md` with Web behavior as the accepted source of truth.
- Confirmed the repository already contains many earlier parity slices, so the first implementation pass will re-audit actual current code rather than reimplement completed work.
- First active slice: AI Provider create/edit UX and the distinction between self-hosted providers and custom models.
- Completed the first slice: the complete Provider editor now opens in a wide scrollable modal with persistent actions; built-in providers, self-hosted providers, and custom models are separate bilingual sections/concepts.
- Red/green structural test passed (5/5 total). Desktop `svelte-check` is 0 errors/0 warnings; Desktop and Web production builds pass.
- Ran browser QA against an isolated temporary service/database, not real user settings. Verified 920px standard layout, 640px responsive layout, scrolling footer visibility, and Escape close. Stopped both preview processes and removed the temporary data directory.
- Updated `features.md`, `prd.md`, `CHANGELOG.md`, and `readme.md`. Remaining parity work is P1 operational detail: model errors, Skill Drafts, Usage/Trace details, Sandbox, Host Bash, System, and runtime installation.
- Completed full Sandbox parity: narrow full-policy PATCH, path/value-safe projection, presets, editable env/network/filesystem rules, reset, fixed save footer, and saved-policy diagnostics refresh.
- Verification: 61 focused server/client tests, 6/6 UI structural tests, Desktop `svelte-check` 0/0, Desktop/Web production builds, and isolated standard/640px browser save-and-reload smoke.
- The isolated preview proved presets remain drafts before save and Strict persists correctly after save. Both local processes were stopped and the temporary data directory was removed.
- Updated required product docs. Remaining P1 scope is now model errors, Skill Drafts, Usage/Trace details, Host Bash, System, and runtime installation.
